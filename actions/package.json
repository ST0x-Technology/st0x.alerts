{
  "name": "sft-tenderly-actions",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild equityPricePushMonitor.ts equityPricePushMonitorTest.ts --bundle --platform=node --format=cjs --external:@tenderly/actions --outdir=out",
    "build:prod": "esbuild equityPricePushMonitor.ts --bundle --platform=node --format=cjs --external:@tenderly/actions --outdir=out",
    "build:test": "esbuild equityPricePushMonitorTest.ts --bundle --platform=node --format=cjs --external:@tenderly/actions --outdir=out"
  },
  "dependencies": {
    "@tenderly/actions": "^0.2.0",
    "esbuild": "^0.24.0",
    "viem": "^2.0.0"
  }
}
